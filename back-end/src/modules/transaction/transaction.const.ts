export enum TransactionSuccessMessage {
  GetTransactionSuccess = "Get list transaction success",
}

export enum TransactionErrorMessage {
  FailedToExecuteTransaction = "Failed to execute transaction",
}
